# Dev/test dependencies for dal_service pytest suite.
# Install: pip install -r requirements-test.txt
# Requires a PostgreSQL database; set TEST_DATABASE_URL (postgresql+asyncpg://...).

fastapi>=0.110.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
python-dotenv>=1.0
pydantic>=2.0

pytest>=8.0
pytest-asyncio>=0.24.0
pytest-cov>=4.0
httpx>=0.27
